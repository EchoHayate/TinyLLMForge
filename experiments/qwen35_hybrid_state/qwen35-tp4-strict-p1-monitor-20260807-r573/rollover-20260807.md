# strict-P1 TP4 monitor rollover r573

- Rolled over from `qwen35-tp4-strict-p1-monitor-20260807-r572`.
- Preserved the complete r572 sample ledger.
- Fixed GPU set remains exactly `2,4,5,6`.
- Poll interval remains `60` seconds.
- Launch gate remains two consecutive `READY` samples.
- Each fixed GPU must have at least `25 GiB` free and no compute
  processes.
- The launch path still runs a fresh strict-P1 preflight before starting
  workers and resumes monitoring if that preflight reports
  `BLOCKED_RESOURCES`.
- The monitor now records cleanup callback exceptions as
  `CLEANUP_FAILED`.
- A successful launch can no longer be reported as successful when
  scoped cleanup fails.
- A launch-preflight race with failed cleanup now stops fail-closed and
  persists `monitor_result.json`.
- The launchd-owned rollover script now exports
  `KRB5CCNAME=FILE:/Users/bytedance/krb5cc_sitian` and owns the
  ControlMaster lifecycle at
  `/tmp/ssh-sitian-10.232.195.203`.
- Existing sample ledgers can be resumed only when they are contiguous,
  parseable, and have no terminal result or failure artifact.
- A resumed process starts with an empty consecutive-READY window, so
  READY observations from before a restart cannot trigger a launch.
- `KeepAlive.SuccessfulExit=false` restarts non-terminal monitor
  failures, while a persisted terminal result lets the service stop
  normally.
- The ControlMaster socket and a real remote resource sample were
  verified after launchd restart.
- Fresh validation passed:
  - ControlMaster transport: 5 tests.
  - strict-P1 monitor: 10 tests.
  - live monitor runner: 6 tests.
  - Python compilation, shell syntax, plist lint, and `git diff --check`.
- First two r573 samples were real remote observations and both were
  `BLOCKED_RESOURCES`.
- At sample 2, fixed GPU state was:
  - GPU 2: `31.56 GiB` free, 6 compute processes.
  - GPU 4: `14.96 GiB` free, 6 compute processes.
  - GPU 5: `46.64 GiB` free, 1 compute process.
  - GPU 6: `27.29 GiB` free, 3 compute processes.
- A real launchd reload preserved the ledger and continued sample ID
  `8 -> 9`.
- A controlled local monitor-process termination produced exit code
  `143`; launchd automatically restarted wrapper PID `52890` as
  `54316`, recreated/retained the ControlMaster, and appended sample
  ID `10` without touching remote compute processes.

No dummy reservation was used and no unrelated remote process was
terminated.
