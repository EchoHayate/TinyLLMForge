# Monitor Rollover

- The original monitor used a 1,440-sample budget at a 60-second interval.
- It remained blocked because fixed GPUs `2,4,5,6` did not simultaneously have zero compute processes.
- Its accumulated `resource_samples.jsonl` is retained unchanged.
- Before budget exhaustion, monitoring was rolled to `qwen35-tp4-strict-p1-monitor-20260807-r572`.
- The replacement keeps the same fixed GPUs, 25 GiB minimum-free threshold, two consecutive READY samples, strict-P1 run tag, manifests, and scoped cleanup behavior.
- The expired dynamic socket was replaced by the already-running verified ControlMaster socket `/tmp/ssh-sitian-10.232.195.203`.
- The replacement sample budget is 10,080 samples, equivalent to seven days at a 60-second interval.
